services:
  yt-dlp-webui:
    build: .
    container_name: yt-dlp-WebUI
    ports:
      - "5000:5000"
    volumes:
      - ./downloads:/app/downloads
    restart: unless-stopped
    networks:
      - default

networks:
  default:
    driver: bridge
